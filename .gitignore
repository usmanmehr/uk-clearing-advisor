# Build artefacts
build/
*.zip

# Logs and temp
*.log
cron.log
run.log
.DS_Store
__pycache__/
*.pyc

# Local secrets / config (never commit)
.env
clearing.env
*.pem
*.key

# Deployment record with live account-specific IDs (keep local, not public)
DEPLOYMENT.md

# Personal denylist for scripts/check_sensitive_content.py - real names,
# account IDs, personal paths. Never commit this file itself (that would
# defeat the point) - see .guardrails/local-denylist.txt.example.
.guardrails/local-denylist.txt

# Local IDE/CLI session state and config - not part of the project, was
# accidentally staged once via `git add -A` which is exactly why it's
# listed here explicitly now rather than relying on it staying untracked.
.kiro/
